

function TransactionDetails(id, vendor, item, category, amount, date) {
    this.id = id;
    this.vendor = vendor;
    this.item = item;
    this.category = category;  
    this.amount = amount;
    this.date = date;
}