
function Transaction(id, itemId, name, vendor, amount, date) {
    this.id = id;
    this.itemId = itemId;
    this.name = name;
    this.vendor = vendor;
    this.amount = amount;
    this.date = date;
}